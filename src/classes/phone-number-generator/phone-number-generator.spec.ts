import { PhoneNumberGenerator } from './phone-number-generator';

describe('PhoneNumberGenerator', () => {
  it('should be defined', () => {
    expect(new PhoneNumberGenerator()).toBeDefined();
  });
});
